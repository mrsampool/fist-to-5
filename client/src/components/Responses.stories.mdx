import { Meta, Story, Canvas } from '@storybook/addon-docs';

<Meta title="Docs/Responses" />

# Responses

The `Responses` component makes an `axios` `GET` request to `api/responses/current`, then uses that data to render a list of `ResponseGroup` components.

<Canvas>
    <Story id="responses--primary" />
</Canvas>
